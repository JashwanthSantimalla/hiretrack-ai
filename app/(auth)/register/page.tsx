import { RegisterForm } from "@/components/forms/RegisterForm";

export default function RegisterPage() {
  return (
    <main className="min-h-screen flex items-start pt-10 justify-center bg-state-50">
      <RegisterForm />
    </main>
  );
}